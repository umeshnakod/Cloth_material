import { FilterDuplicateItemPipe } from './filter-duplicate-item.pipe';

describe('FilterDuplicateItemPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDuplicateItemPipe();
    expect(pipe).toBeTruthy();
  });
});
